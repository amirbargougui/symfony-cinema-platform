{% extends 'base.html.twig' %}

{% block title %}Edit Sieges{% endblock %}

{% block body %}
    <h1>Edit Sieges</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Update</button>
    <a href="{{ path('app_sieges_index') }}">Back to list</a>
    {{ include('sieges/_delete_form.html.twig') }}

    {{ form_end(form) }}
{% endblock %}
