<form method="post" action="{{ path('app_categories_delete', {'categorieid': category.categorieid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.categorieid) }}">
    <button class="btn">Delete</button>
</form>
